---
phase: 17-npm-run-scripts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/api.json
  - scripts/start-api.mjs
  - package.json
autonomous: true
requirements:
  - SCRIPT-02

must_haves:
  truths:
    - 'Developer can run `npm run api` to start the API server'
    - 'API server initializes database schema on startup'
    - 'API server listens on port from config file'
    - 'API server serves HTTP endpoints for task queue, status, context'
    - 'API server handles graceful shutdown closing database connections'
  artifacts:
    - path: 'config/api.json'
      provides: 'API server configuration with port and database path'
      contains: 'port, dbPath'
    - path: 'scripts/start-api.mjs'
      provides: 'API server runner with database initialization and graceful shutdown'
      exports: ['Config loading', 'Database initialization', 'Server startup', 'Shutdown handlers']
    - path: 'package.json'
      provides: 'npm script entry point for API server'
      contains: '"api": "node scripts/start-api.mjs"'
  key_links:
    - from: 'scripts/start-api.mjs'
      to: 'packages/coordination/src/api/server.ts'
      via: 'Import createStateApi, startServer, stopServer functions'
      pattern: 'createStateApi.*startServer'
    - from: 'scripts/start-api.mjs'
      to: 'packages/coordination/src/state/index.ts'
      via: 'Import createDatabase, initializeSchema for database setup'
      pattern: 'createDatabase.*initializeSchema'
    - from: 'scripts/start-api.mjs'
      to: 'config/api.json'
      via: 'Load port and dbPath from JSON config'
      pattern: "config.*api\\.json"
---

<objective>
Create npm run api script that starts the API server with automatic database initialization.

Purpose: Enable developers to start the coordination REST API with a single command, including automatic database schema initialization.

Output: Working `npm run api` command that launches the API server with database setup.
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-npm-run-scripts/17-CONTEXT.md
@.planning/phases/17-npm-run-scripts/17-RESEARCH.md

@packages/coordination/src/api/server.ts
@packages/coordination/src/state/index.ts
@scripts/setup.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API server configuration</name>
  <files>config/api.json</files>
  <action>
    Create config/api.json with API server settings:

    {
      "port": 3000,
      "dbPath": "./packages/coordination/swarm.db"
    }

    Per 17-CONTEXT.md: Port specified from shared config file.
    Per 17-RESEARCH.md Pitfall 2: Port conflict handling - fail fast with clear error message.

  </action>
  <verify>
    <automated>node -e "const cfg = require('./config/api.json'); console.log('port:', cfg.port, 'dbPath:', cfg.dbPath)"</automated>
    <manual>Config file exists with port and dbPath fields</manual>
  </verify>
  <done>config/api.json exists with valid JSON and required fields</done>
</task>

<task type="auto">
  <name>Task 2: Create start-api.mjs script</name>
  <files>scripts/start-api.mjs</files>
  <action>
    Create scripts/start-api.mjs following setup.mjs pattern:

    #!/usr/bin/env node
    /**
     * Start API Server Script
     *
     * Usage: npm run api [options]
     * Options:
     *   --config <path>   Path to API config file (default: config/api.json)
     *   -q, --quiet       Silence output
     *   -v, --verbose     Enable verbose logging
     */

    import { $ } from 'zx';
    import chalk from 'chalk';
    import path from 'node:path';
    import { fileURLToPath } from 'node:url';
    import { readFile } from 'node:fs/promises';
    import minimist from 'minimist';

    const __dirname = path.dirname(fileURLToPath(import.meta.url));
    $.verbose = false;

    // Parse CLI arguments per 17-CONTEXT.md
    const args = minimist(process.argv.slice(2), {
      alias: {
        quiet: ['q'],
        verbose: ['v'],
        config: ['c'],
      },
      default: {
        config: path.join(__dirname, '..', 'config', 'api.json'),
      },
    });

    // Logging helper
    let logLevel = args.quiet ? 'none' : (args.verbose ? 'verbose' : 'info');

    function log(message, level = 'info') {
      if (logLevel === 'none') return;
      if (level === 'error' || logLevel === 'verbose') {
        console[level === 'error' ? chalk.red(message) : message]);
      } else if (level === 'info') {
        console.log(message);
      }
    }

    async function main() {
      // Load and validate config (fail-fast per 17-CONTEXT.md)
      const configPath = args.config;
      let config;
      try {
        const configContent = await fs.readFile(configPath, 'utf-8');
        config = JSON.parse(configContent);
      } catch (error) {
        console.error(chalk.red('Error loading config:'), error.message);
        console.error(chalk.yellow('Fix: Ensure config file exists at ' + configPath));
        process.exit(1);
      }

      // Validate required fields
      if (!config.port || !config.dbPath) {
        console.error(chalk.red('Missing required fields: port, dbPath'));
        console.error(chalk.yellow('Fix: Add these fields to ' + configPath));
        process.exit(1);
      }

      // Resolve dbPath relative to repo root
      const dbPath = path.resolve(__dirname, '..', config.dbPath);

      log(chalk.bold('Starting API server...'));

      // Import from coordination package (SCRIPT-02: automatic database initialization)
      const { createDatabase, initializeSchema } = await import('../packages/coordination/dist/state/index.js');
      const { createStateApi, startServer, stopServer } = await import('../packages/coordination/dist/api/server.js');

      // Initialize database
      let db;
      try {
        db = createDatabase({ dbPath });
        initializeSchema(db);
        log(chalk.green('✓ Database initialized'), 'info');
      } catch (error) {
        console.error(chalk.red('Failed to initialize database:'), error.message);
        console.error(chalk.yellow('Fix: Ensure better-sqlite3 is installed and path is writable'));
        process.exit(1);
      }

      // Create Express app
      const app = createStateApi(db);

      // Start server
      let server;
      try {
        server = startServer(app, config.port);
        log(chalk.green(`✓ API server listening on port ${config.port}`), 'info');

        // Health check verification
        const response = await fetch(`http://localhost:${config.port}/health`);
        if (response.ok) {
          const health = await response.json();
          log(chalk.green('✓ Health check passed'), 'verbose');
          if (logLevel === 'verbose') {
            log(JSON.stringify(health, null, 2), 'verbose');
          }
        }
      } catch (error) {
        // Port conflict handling per 17-RESEARCH.md Pitfall 2
        if (error.code === 'EADDRINUSE') {
          console.error(chalk.red(`Error: Port ${config.port} is already in use`));
          console.error(chalk.yellow('Fix: Stop the existing process or change the port in config/api.json'));
        } else {
          console.error(chalk.red('Failed to start server:'), error.message);
        }
        db.close();
        process.exit(1);
      }

      // Graceful shutdown per 17-CONTEXT.md
      const shutdown = async (signal) => {
        log(chalk.yellow(`\nReceived ${signal}, shutting down gracefully...`));
        await stopServer(server);
        db.close();
        log(chalk.green('✓ API server stopped'));
        process.exit(0);
      };

      process.on('SIGTERM', () => shutdown('SIGTERM'));
      process.on('SIGINT', () => shutdown('SIGINT'));
    }

    main().catch(error => {
      console.error(chalk.red('API server error:'), error);
      process.exit(1);
    });

    Key patterns from 17-RESEARCH.md:
    - Use zx for shell operations
    - Use minimist for CLI flag parsing
    - Use chalk for colored terminal output
    - Initialize database schema on startup (SCRIPT-02 requirement)
    - Port conflict handling with clear Fix: message
    - Graceful shutdown with database cleanup

  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test for start-api.mjs (mock database, verify server startup, check health endpoint)</automated>
    <manual>Script accepts --config flag, validates missing config, initializes database, and has shutdown handlers</manual>
  </verify>
  <done>start-api.mjs exists with CLI parsing, database initialization, server startup, and graceful shutdown</done>
</task>

<task type="auto">
  <name>Task 3: Add npm run api script to package.json</name>
  <files>package.json</files>
  <action>
    Add "api": "node scripts/start-api.mjs" to root package.json scripts section.

    Per 17-RESEARCH.md Pattern 1: npm workspace script delegation.
    Place after "agent" script from 17-01.

    Verify no conflicts with existing scripts.

  </action>
  <verify>
    <automated>npm run api --help 2>&1 | grep -q "Usage:" || echo "Script entry point exists"</automated>
    <manual>package.json has "api" script pointing to scripts/start-api.mjs</manual>
  </verify>
  <done>npm run api launches start-api.mjs script</done>
</task>

</tasks>

<verification>
Overall phase checks:

1. Run `npm run api` from monorepo root
2. Verify database schema is initialized (check for .db file created)
3. Verify API server listens on configured port (default 3000)
4. Test /health endpoint returns 200 with valid status
5. Test /api/tasks endpoint returns task queue (empty or populated)
6. Send SIGINT (Ctrl+C) and verify graceful shutdown
7. Try with port already in use and verify clear error message with Fix:
8. Try with --config flag pointing to custom config

Expected behaviors:

- API server starts with config from config/api.json
- Database schema is initialized automatically
- Health endpoint returns 200 status
- Graceful shutdown closes database and server
- Clear error messages for port conflicts
  </verification>

<success_criteria>
Developer can run `npm run api` to start the API server that:

1. Loads configuration from config/api.json
2. Initializes database schema automatically
3. Starts HTTP server on configured port
4. Serves API endpoints (tasks, status, context, health)
5. Handles graceful shutdown closing database and server

All verification steps pass without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/17-npm-run-scripts/17-02-SUMMARY.md`
</output>
