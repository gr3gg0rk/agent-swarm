---
phase: 11-opt-in-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/src/optimization/config.ts
  - packages/coordination/src/optimization/index.ts
  - examples/basic-agent.ts
  - examples/config.yaml
  - docs/optimization.md
  - packages/coordination/test/activation.test.ts
autonomous: true
requirements: [OPTI-01, OPTI-02, OPTI-03, OPTI-04]

must_haves:
  truths:
    - "MessageBatcher is wired to MQTT client on coordinator startup when SWARM_BATCHING_ENABLED != 'false'"
    - "ConnectionPool is activated in MQTT client initialization when SWARM_POOLING_ENABLED != 'false'"
    - "Feature flags allow toggling batching/pooling via environment variables for debugging"
    - "Documentation describes opt-in configuration options (SWARM_BATCHING_ENABLED, SWARM_POOLING_ENABLED)"
  artifacts:
    - path: "packages/coordination/src/optimization/config.ts"
      provides: "Environment variable loading for optimization flags"
      exports: ["loadOptimizationConfig", "OptimizationConfig"]
    - path: "examples/basic-agent.ts"
      provides: "Agent initialization with optimization activation"
      contains: "loadOptimizationConfig"
    - path: "docs/optimization.md"
      provides: "Feature activation documentation"
      contains: "SWARM_BATCHING_ENABLED"
    - path: "packages/coordination/test/activation.test.ts"
      provides: "Integration tests for optimization activation"
      exports: ["activation test suite"]
  key_links:
    - from: "examples/basic-agent.ts"
      to: "packages/coordination/src/optimization/config.ts"
      via: "import loadOptimizationConfig"
      pattern: "loadOptimizationConfig"
    - from: "examples/basic-agent.ts"
      to: "MqttClient.setBatchPublisher"
      via: "mqttClient.setBatchPublisher(batcher)"
      pattern: "setBatchPublisher"
    - from: "examples/basic-agent.ts"
      to: "MqttClient.setConnectionPool"
      via: "mqttClient.setConnectionPool(pool)"
      pattern: "setConnectionPool"
---

<objective>
Activate MessageBatcher and ConnectionPool by default for production use, with environment variable feature flags for debugging. Wire the optimization features from Phase 7 into agent initialization flow.

Purpose: Close BATCHER-NOT-ACTIVATED and POOL-NOT-ACTIVATED gaps from v1.1 audit by providing a production-ready activation pattern with sensible defaults.
Output: OptimizationConfig module, updated basic-agent.ts with optimization activation, documentation, and integration tests.
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-opt-in-activation/11-RESEARCH.md
@.planning/phases/07-optimization/07-01-SUMMARY.md
@.planning/phases/07-optimization/07-02-SUMMARY.md
@packages/coordination/src/communication/mqtt.ts
@packages/coordination/src/optimization/batcher.ts
@packages/coordination/src/optimization/connection-pool.ts
@examples/basic-agent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OptimizationConfig module with environment variable loading</name>
  <files>packages/coordination/src/optimization/config.ts</files>
  <action>Create packages/coordination/src/optimization/config.ts with:

1. Export interface OptimizationConfig with:
   - batchingEnabled: boolean (default: true)
   - poolingEnabled: boolean (default: true)
   - Optional batchConfig?: BatchConfig for custom thresholds

2. Export function loadOptimizationConfig(): OptimizationConfig that:
   - Reads process.env.SWARM_BATCHING_ENABLED (default: true if unset or 'true', false if 'false')
   - Reads process.env.SWARM_POOLING_ENABLED (default: true if unset or 'true', false if 'false')
   - Returns config object with parsed boolean values

DO NOT use dotenv or any external libraries - use only process.env built-in. Per 11-RESEARCH.md, environment variables are the standard configuration pattern.

Reference: 11-RESEARCH.md Pattern 1 shows the exact interface structure needed.
</action>
  <verify>
    <automated>grep -q "export.*loadOptimizationConfig" packages/coordination/src/optimization/config.ts && grep -q "SWARM_BATCHING_ENABLED" packages/coordination/src/optimization/config.ts && grep -q "SWARM_POOLING_ENABLED" packages/coordination/src/optimization/config.ts</automated>
  </verify>
  <done>OptimizationConfig interface exists, loadOptimizationConfig() reads SWARM_BATCHING_ENABLED and SWARM_POOLING_ENABLED from process.env with correct defaults</done>
</task>

<task type="auto">
  <name>Task 2: Update optimization module exports and example agent initialization</name>
  <files>packages/coordination/src/optimization/index.ts examples/basic-agent.ts examples/config.yaml</files>
  <action>Update three files:

1. packages/coordination/src/optimization/index.ts:
   - Add exports: OptimizationConfig, loadOptimizationConfig
   - Import from './config.js'

2. examples/basic-agent.ts main() function:
   - Import loadOptimizationConfig, MessageBatcher, ConnectionPoolManager from coordination package
   - After mqttClient = await connectToBroker(brokerConfig), call loadOptimizationConfig()
   - If optConfig.batchingEnabled, create MessageBatcher and call mqttClient.setBatchPublisher(batcher)
   - If optConfig.poolingEnabled, create ConnectionPoolManager and call mqttClient.setConnectionPool(pool)
   - Add console.log statements to indicate which features are activated
   - In stop() method, ensure connectionPool.stop() is called if pool exists

3. examples/config.yaml:
   - Add comments section explaining SWARM_BATCHING_ENABLED and SWARM_POOLING_ENABLED environment variables
   - Document default values (both enabled) and how to disable for debugging

Per 11-RESEARCH.md Pattern 2: Create and configure MessageBatcher and ConnectionPool before use, activate via setBatchPublisher/setConnectionPool methods.
</action>
  <verify>
    <automated>grep -q "loadOptimizationConfig" examples/basic-agent.ts && grep -q "setBatchPublisher" examples/basic-agent.ts && grep -q "setConnectionPool" examples/basic-agent.ts && grep -q "SWARM_BATCHING_ENABLED" examples/config.yaml</automated>
  </verify>
  <done>basic-agent.ts wires MessageBatcher and ConnectionPool when enabled, config.yaml documents environment variables, optimization module exports config functions</done>
</task>

<task type="auto">
  <name>Task 3: Create documentation and integration tests for activation</name>
  <files>docs/optimization.md packages/coordination/test/activation.test.ts</files>
  <action>Create two files:

1. docs/optimization.md with sections:
   - Overview: Message batching and connection pooling for improved throughput
   - Activation: Default enabled, environment variables to disable
   - Environment Variables:
     * SWARM_BATCHING_ENABLED=true|false (default: true)
     * SWARM_POOLING_ENABLED=true|false (default: true)
   - Configuration: How to customize batch thresholds via OptimizationConfig
   - Debugging: How to disable features individually to isolate issues
   - Performance: Expected 10x throughput improvement (per OPTI-01)

2. packages/coordination/test/activation.test.ts with tests:
   - test("loadOptimizationConfig returns defaults when env vars unset"): Verify batchingEnabled=true, poolingEnabled=true
   - test("loadOptimizationConfig respects SWARM_BATCHING_ENABLED=false"): Verify batchingEnabled=false when set
   - test("loadOptimizationConfig respects SWARM_POOLING_ENABLED=false"): Verify poolingEnabled=false when set
   - test("MessageBatcher activation via setBatchPublisher"): Verify mqttClient.getBatchPublisher() returns batcher after set
   - test("ConnectionPool activation via setConnectionPool"): Verify mqttClient.getConnectionPool() returns pool after set

Use Node.js built-in test runner (node:test) per project conventions. Import MqttClient, MessageBatcher, ConnectionPoolManager from dist/ for testing.

Per 11-RESEARCH.md Wave 0 Gaps: Test file covers OPTI-01, OPTI-03 integration tests.
</action>
  <verify>
    <automated>node --test packages/coordination/test/activation.test.ts</automated>
  </verify>
  <done>Documentation describes all environment variables and activation options, integration tests verify default behavior and feature flag toggles</done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Run integration tests: `node --test packages/coordination/test/activation.test.ts` - all tests pass
2. Verify basic-agent.ts compiles: `npx tsc --noEmit examples/basic-agent.ts` - no TypeScript errors
3. Verify environment variable handling: Test with SWARM_BATCHING_ENABLED=false and SWARM_POOLING_ENABLED=false set
4. Verify documentation completeness: docs/optimization.md describes all environment variables and configuration options
5. Verify defaults: When env vars unset, optimizations are enabled by default (production-safe)
</verification>

<success_criteria>
Phase complete when:
1. loadOptimizationConfig() function reads SWARM_BATCHING_ENABLED and SWARM_POOLING_ENABLED with correct defaults
2. examples/basic-agent.ts activates MessageBatcher and ConnectionPool based on config
3. docs/optimization.md documents environment variables and debugging options
4. Integration tests verify activation flow for both features
5. All tests pass with no TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-opt-in-activation/11-01-SUMMARY.md`
</output>
