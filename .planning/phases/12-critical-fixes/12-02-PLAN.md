---
phase: 12-critical-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/src/index.ts
autonomous: true
requirements:
  - CRIT-03
user_setup: []

must_haves:
  truths:
    - "MessageBatcher can be imported from @openclaw-swarm/coordination"
    - "ConnectionPoolManager can be imported from @openclaw-swarm/coordination"
    - "loadOptimizationConfig can be imported from @openclaw-swarm/coordination"
    - "All optimization exports available via flat import path"
  artifacts:
    - path: "packages/coordination/src/index.ts"
      provides: "Main package exports including optimization module"
      contains: "export * from './optimization/index.js'"
  key_links:
    - from: "packages/coordination/src/index.ts"
      to: "packages/coordination/src/optimization/index.ts"
      via: "export * re-export"
      pattern: "export.*from.*optimization"
---

<objective>
Add optimization module exports to main package index for flat import paths.

Purpose: The optimization module (MessageBatcher, ConnectionPoolManager, loadOptimizationConfig) exists in src/optimization/index.ts but is not re-exported from the main index.ts. This forces users to use deep import paths. Adding the re-export enables the standard import pattern.

Output: Main index.ts re-exports optimization module, all optimization symbols importable from @openclaw-swarm/coordination.
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-fixes/12-CONTEXT.md
@.planning/phases/12-critical-fixes/12-RESEARCH.md
@packages/coordination/src/index.ts
@packages/coordination/src/optimization/index.ts
</context>

<tasks>

<task type="auto">
  <name>Add optimization module re-export to main index</name>
  <files>packages/coordination/src/index.ts</files>
  <action>
    Add the following line after the memory module export (after line 33):

    ```typescript
    // Re-export all optimization types and functions
    export * from './optimization/index.js';
    ```

    Per 12-RESEARCH.md Pattern 3: Module Re-exports in TypeScript.
    Per 12-CONTEXT.md decision: Fix symptoms (add missing export) not root causes.
  </action>
  <verify>
    <automated>cd packages/coordination && npm run build</automated>
    <sampling_rate>run after task completes, before next task</sampling_rate>
  </verify>
  <done>
    - index.ts includes "export * from './optimization/index.js'"
    - Build completes without errors
    - Optimization symbols available from main export
  </done>
</task>

</tasks>

<verification>
1. Export added: `grep "export.*from.*optimization" packages/coordination/src/index.ts` finds the re-export line
2. Build succeeds: `cd packages/coordination && npm run build` returns exit code 0
3. Dist includes optimization: `ls packages/coordination/dist/optimization/` shows index.js exists
</verification>

<success_criteria>
Developers can import MessageBatcher, ConnectionPoolManager, and loadOptimizationConfig directly from @openclaw-swarm/coordination without using deep import paths.
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes/12-02-SUMMARY.md`
</output>
