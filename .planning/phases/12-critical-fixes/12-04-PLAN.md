---
phase: 12-critical-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/src/state/database.ts
autonomous: true
requirements:
  - CRIT-05
user_setup: []

must_haves:
  truths:
    - "pragma calls use { simple: true } option for string return values"
    - "journal_mode pragma returns 'wal' string not Database object"
    - "Database WAL mode verification works correctly"
    - "getDatabaseStats returns properly typed values"
  artifacts:
    - path: "packages/coordination/src/state/database.ts"
      provides: "Database connection with correct pragma return types"
      fixes:
        - "line 76: Add { simple: true } to journal_mode pragma"
        - "line 77: Compare result to string 'wal'"
        - "lines 177-179: Add { simple: true } to getDatabaseStats pragmas"
  key_links:
    - from: "packages/coordination/src/state/database.ts"
      to: "better-sqlite3@11.10.0"
      via: "pragma calls with simple option"
      pattern: "db\\.pragma.*simple.*true"
---

<objective>
Fix database pragma calls to use { simple: true } option for proper string return values.

Purpose: better-sqlite3 returns array of objects by default. For single-value pragmas like journal_mode, the { simple: true } option returns primitive values instead. Current code on line 76 compares result to 'wal' string but gets a Database object without the simple option, causing comparison failures.

Output: All pragma calls that need single values use { simple: true }, comparisons work correctly.
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-fixes/12-CONTEXT.md
@.planning/phases/12-critical-fixes/12-RESEARCH.md
@packages/coordination/src/state/database.ts
</context>

<tasks>

<task type="auto">
  <name>Add simple option to pragma calls for string return values</name>
  <files>packages/coordination/src/state/database.ts</files>
  <action>
    1. Line 76: Change `const result = db.pragma('journal_mode = WAL');` to `const result = db.pragma('journal_mode = WAL', { simple: true });`
    2. Line 77: Update error message from `throw new Error(\`Failed to enable WAL mode: got ${result}\`);` to `throw new Error(\`Pragma journal_mode failed: expected WAL, got ${result}. Fix: Ensure database directory is writable and not on network filesystem.\`);` per 12-CONTEXT.md error handling pattern

    Note: Lines 177-179 already use { simple: true } correctly - no changes needed there.

    Per 12-RESEARCH.md Pattern 2: better-sqlite3 Pragma with Simple Option.
    Per 12-CONTEXT.md decision: Include actionable "Fix:" suggestions in error messages.
  </action>
  <verify>
    <automated>cd packages/coordination && npm run build</automated>
    <sampling_rate>run after task completes, before next task</sampling_rate>
  </verify>
  <done>
    - Line 76 pragma includes { simple: true } option
    - Error message includes "Fix:" suggestion
    - Build completes without errors
  </done>
</task>

</tasks>

<verification>
1. Simple option added: `grep "pragma.*journal_mode.*simple.*true" packages/coordination/src/state/database.ts` finds the pragma call
2. Fix suggestion in error: `grep "Fix:" packages/coordination/src/state/database.ts` finds actionable error message
3. Build succeeds: `cd packages/coordination && npm run build` returns exit code 0
</verification>

<success_criteria>
Database pragma calls that return single values use the { simple: true } option, ensuring proper string return types. The journal_mode comparison works correctly, and error messages include actionable Fix suggestions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes/12-04-SUMMARY.md`
</output>
