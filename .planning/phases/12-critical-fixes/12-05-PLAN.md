---
phase: 12-critical-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/src/state/task-queue.ts
autonomous: true
requirements:
  - CRIT-06
user_setup: []

must_haves:
  truths:
    - "INSERT statement has 15 placeholders for 15 columns"
    - "TaskQueue.createTask executes without parameter count errors"
    - "All task fields bind correctly to placeholders"
  artifacts:
    - path: "packages/coordination/src/state/task-queue.ts"
      provides: "Task queue with correct SQL placeholder count"
      fixes:
        - "line 69: Remove extra placeholder (16th ?) to match 15 columns"
  key_links:
    - from: "packages/coordination/src/state/task-queue.ts"
      to: "better-sqlite3@11.10.0"
      via: "prepared statement with matching placeholder count"
      pattern: "INSERT INTO tasks.*VALUES.*\\?.*\\?"
---

<objective>
Fix SQL INSERT statement to have correct number of placeholders (15) matching column count.

Purpose: The INSERT statement in task-queue.ts line 69 has 16 placeholders but only 15 columns. This causes "wrong number of parameters" runtime errors when executing prepared statements. Remove one placeholder to match the column count.

Output: INSERT statement has 15 placeholders for 15 columns, createTask executes without errors.
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-fixes/12-CONTEXT.md
@.planning/phases/12-critical-fixes/12-RESEARCH.md
@packages/coordination/src/state/task-queue.ts
</context>

<tasks>

<task type="auto">
  <name>Fix INSERT statement placeholder count</name>
  <files>packages/coordination/src/state/task-queue.ts</files>
  <action>
    Line 69: Change `VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)` to `VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`

    Remove the 16th placeholder to match the 15 columns specified in the INSERT statement:
    1. id
    2. status
    3. priority
    4. assigned_agent
    5. created_at
    6. updated_at
    7. completed_at
    8. payload
    9. dependencies
    10. timeout_ms
    11. retry_count
    12. max_retries
    13. last_progress_at
    14. result_payload
    15. error_type

    Per 12-RESEARCH.md Pitfall 3: SQL Placeholder Count Mismatch.
    Per 12-CONTEXT.md decision: Throw immediately with details on SQL placeholder mismatch.
  </action>
  <verify>
    <automated>cd packages/coordination && npm run build</automated>
    <sampling_rate>run after task completes, before next task</sampling_rate>
  </verify>
  <done>
    - Line 69 has exactly 15 placeholders
    - Build completes without errors
    - Column count matches placeholder count
  </done>
</task>

</tasks>

<verification>
1. Placeholder count: `grep -o "?" packages/coordination/src/state/task-queue.ts | grep -A 15 "INSERT INTO tasks" | wc -l` shows 15 in the INSERT VALUES clause
2. Column count matches: `grep "INSERT INTO tasks" packages/coordination/src/state/task-queue.ts | grep -o "," | wc -l` shows 14 commas = 15 columns
3. Build succeeds: `cd packages/coordination && npm run build` returns exit code 0
</verification>

<success_criteria>
INSERT statement has 15 placeholders matching the 15 columns. TaskQueue.createTask executes without "wrong number of parameters" runtime errors.
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes/12-05-SUMMARY.md`
</output>
