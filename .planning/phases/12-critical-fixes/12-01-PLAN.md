---
phase: 12-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/src/communication/codec.ts
autonomous: true
requirements:
  - CRIT-01
  - CRIT-02
user_setup: []

must_haves:
  truths:
    - "msgpackr imports use pack/unpack functions instead of MessagePack class"
    - "No @ts-ignore comments in codec.ts"
    - "encodeMessage uses pack() for MessagePack encoding"
    - "decodeMessage uses unpack() for MessagePack decoding"
    - "npm run build completes without TypeScript errors"
  artifacts:
    - path: "packages/coordination/src/communication/codec.ts"
      provides: "Message codec with functional msgpackr API"
      exports: ["encodeMessage", "decodeMessage", "shouldUseMessagePack"]
      no_ts_ignore: true
  key_links:
    - from: "packages/coordination/src/communication/codec.ts"
      to: "msgpackr@0.6.0"
      via: "import { pack, unpack } from 'msgpackr'"
      pattern: "import.*pack.*unpack.*from 'msgpackr'"
---

<objective>
Fix msgpackr imports to use the correct functional API (pack/unpack) instead of the non-existent MessagePack class.

Purpose: The msgpackr 0.6.0 package exports `pack` and `unpack` functions, NOT a `MessagePack` class. Current code uses `@ts-ignore` to suppress type errors, causing runtime failures. This fix enables successful package installation and import.

Output: Working codec.ts with functional msgpackr API, build succeeds, no @ts-ignore comments.
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-fixes/12-CONTEXT.md
@.planning/phases/12-critical-fixes/12-RESEARCH.md
@packages/coordination/src/communication/codec.ts
</context>

<tasks>

<task type="auto">
  <name>Replace MessagePack class with pack/unpack functions</name>
  <files>packages/coordination/src/communication/codec.ts</files>
  <action>
    1. Remove the `@ts-ignore` comment on line 12
    2. Change import from `import { MessagePack } from 'msgpackr'` to `import { pack, unpack } from 'msgpackr'`
    3. Update line 69: Replace `MessagePack.encode(codecEnvelope)` with `pack(codecEnvelope)`
    4. Update line 96: Replace `MessagePack.decode(buffer)` with `unpack(buffer)`
    5. Update line 108: Replace `MessagePack.decode(buffer)` with `unpack(buffer)`

    Per 12-RESEARCH.md Pattern 1: msgpackr 0.6.0 exports pack/unpack functions, not MessagePack class.
    Per 12-CONTEXT.md decision: Clean break acceptable - use only pack/unpack functions.
  </action>
  <verify>
    <automated>cd packages/coordination && npm run build</automated>
    <sampling_rate>run after task completes, before next task</sampling_rate>
  </verify>
  <done>
    - codec.ts imports pack/unpack from msgpackr
    - No @ts-ignore comments in codec.ts
    - encodeMessage uses pack() for encoding
    - decodeMessage uses unpack() for decoding
    - Build completes without errors
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd packages/coordination && npm run build` returns exit code 0
2. No @ts-ignore in codec.ts: `grep -n "@ts-ignore" packages/coordination/src/communication/codec.ts` returns empty
3. Correct imports: `grep "import.*pack.*unpack.*from 'msgpackr'" packages/coordination/src/communication/codec.ts` finds the import line
4. pack() used: `grep "pack(" packages/coordination/src/communication/codec.ts` finds usage in encodeMessage
5. unpack() used: `grep "unpack(" packages/coordination/src/communication/codec.ts` finds usage in decodeMessage
</verification>

<success_criteria>
Developer can run `npm install && npm run build` in coordination package without msgpackr-related import errors. The codec.ts file uses the correct functional API (pack/unpack) instead of the non-existent MessagePack class.
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes/12-01-SUMMARY.md`
</output>
