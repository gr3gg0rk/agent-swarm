---
phase: 12-critical-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/src/index.ts
autonomous: true
requirements:
  - CRIT-04
user_setup: []

must_haves:
  truths:
    - "initializeSchema can be imported from @openclaw-swarm/coordination"
    - "validateSchema can be imported from @openclaw-swarm/coordination"
    - "Schema functions available via flat import path"
  artifacts:
    - path: "packages/coordination/src/index.ts"
      provides: "Main package exports including schema functions"
      contains: "export { initializeSchema, validateSchema } from './state/schema.js'"
  key_links:
    - from: "packages/coordination/src/index.ts"
      to: "packages/coordination/src/state/schema.ts"
      via: "named export re-export"
      pattern: "export.*initializeSchema.*validateSchema.*from.*schema"
---

<objective>
Add schema function exports to main package index for flat import paths.

Purpose: The schema functions (initializeSchema, validateSchema) are exported from state/schema.ts and re-exported from state/index.ts, but not directly from the main package index.ts. Adding these exports enables developers to import them directly from @openclaw-swarm/coordination.

Output: Main index.ts re-exports initializeSchema and validateSchema, both functions importable from @openclaw-swarm/coordination.
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-fixes/12-CONTEXT.md
@.planning/phases/12-critical-fixes/12-RESEARCH.md
@packages/coordination/src/index.ts
@packages/coordination/src/state/schema.ts
@packages/coordination/src/state/index.ts
</context>

<tasks>

<task type="auto">
  <name>Add schema function exports to main index</name>
  <files>packages/coordination/src/index.ts</files>
  <action>
    Add the following line after the optimization module export (after the line added in 12-02):

    ```typescript
    // Re-export schema functions for convenience
    export { initializeSchema, validateSchema } from './state/schema.js';
    ```

    Per 12-RESEARCH.md Pattern 3: Module Re-exports in TypeScript.
    Per 12-CONTEXT.md decision: Fix symptoms (add missing export) not root causes.
    Note: Using named export instead of wildcard for cleaner API and to avoid re-exporting internal functions.
  </action>
  <verify>
    <automated>cd packages/coordination && npm run build</automated>
    <sampling_rate>run after task completes, before next task</sampling_rate>
  </verify>
  <done>
    - index.ts includes "export { initializeSchema, validateSchema } from './state/schema.js'"
    - Build completes without errors
    - Schema functions available from main export
  </done>
</task>

</tasks>

<verification>
1. Export added: `grep "export.*initializeSchema.*validateSchema.*from.*schema" packages/coordination/src/index.ts` finds the export line
2. Build succeeds: `cd packages/coordination && npm run build` returns exit code 0
3. Functions in dist: `grep "initializeSchema\|validateSchema" packages/coordination/dist/index.d.ts` finds both functions
</verification>

<success_criteria>
Developers can import initializeSchema and validateSchema directly from @openclaw-swarm/coordination without using deep import paths.
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes/12-03-SUMMARY.md`
</output>
