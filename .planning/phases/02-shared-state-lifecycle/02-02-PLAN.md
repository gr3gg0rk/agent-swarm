---
phase: 02-shared-state-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/src/lifecycle/heartbeat.ts
  - packages/coordination/src/lifecycle/supervisor.ts
  - packages/coordination/src/lifecycle/shutdown.ts
  - packages/coordination/src/lifecycle/index.ts
  - packages/coordination/src/index.ts
  - packages/coordination/src/communication/topics.ts
  - config/supervisor/openclaw-agent@.service
autonomous: true
requirements:
  - DISC-04
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - STAT-01

must_haves:
  truths:
    - "Agents publish heartbeat messages every 30 seconds with status (idle/busy/error)"
    - "Agent missing 4 consecutive heartbeats is marked offline (2-minute timeout)"
    - "Agents start automatically on machine boot via systemd"
    - "Agents that crash are automatically restarted with exponential backoff"
    - "Agents gracefully shutdown on SIGTERM, completing current task if possible"
    - "Heartbeat tracking state persists in database for crash recovery"
  artifacts:
    - path: "packages/coordination/src/lifecycle/heartbeat.ts"
      provides: "Heartbeat publisher and tracker with 4-miss offline detection"
      exports: ["HeartbeatPublisher", "HeartbeatTracker", "createHeartbeatPublisher", "createHeartbeatTracker"]
      min_lines: 120
    - path: "packages/coordination/src/lifecycle/supervisor.ts"
      provides: "systemd service file template for agent supervision"
      exports: ["generateSystemdService", "SYSTEMD_TEMPLATE"]
      min_lines: 80
    - path: "packages/coordination/src/lifecycle/shutdown.ts"
      provides: "Graceful shutdown handler with SIGTERM/SIGINT handling"
      exports: ["GracefulShutdown", "createGracefulShutdown"]
      min_lines: 80
    - path: "packages/coordination/src/communication/topics.ts"
      provides: "Heartbeat topic factory function"
      exports: ["agentHeartbeat"]
      contains: "agentHeartbeat"
    - path: "config/supervisor/openclaw-agent@.service"
      provides: "systemd service template with Restart=on-failure and exponential backoff"
      min_lines: 30
  key_links:
    - from: "packages/coordination/src/lifecycle/heartbeat.ts"
      to: "packages/coordination/src/communication/mqtt.ts"
      via: "MQTT client import for publishing heartbeats"
      pattern: "import.*MqttClient.*from.*communication\\/mqtt"
    - from: "packages/coordination/src/lifecycle/heartbeat.ts"
      to: "packages/coordination/src/state/database.ts"
      via: "Database import for persisting heartbeat state"
      pattern: "import.*Database.*from.*state\\/database"
    - from: "packages/coordination/src/lifecycle/shutdown.ts"
      to: "packages/coordination/src/discovery/registry.ts"
      via: "AgentDiscovery import for unregistration on shutdown"
      pattern: "import.*AgentDiscovery.*from.*discovery\\/registry"
    - from: "config/supervisor/openclaw-agent@.service"
      to: "systemd"
      via: "Restart=on-failure and RestartSteps configuration"
      pattern: "Restart=on-failure"
---

<objective>
Heartbeat monitoring with 30-second publishing interval, 4-miss offline detection, systemd service templates for auto-restart with exponential backoff, and graceful shutdown handling for task completion.

Purpose: Enables agents to publish health status, detect offline agents automatically, ensures crashed agents restart, and allows graceful shutdown for in-progress task completion.

Output: Heartbeat publisher/tracker, systemd service template, graceful shutdown handler, heartbeat topic factory.
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-state-lifecycle/02-CONTEXT.md
@.planning/phases/02-shared-state-lifecycle/02-RESEARCH.md
@.planning/phases/01-communication-discovery/01-01-SUMMARY.md
@.planning/phases/01-communication-discovery/01-02-SUMMARY.md

# Reference existing patterns
@packages/coordination/src/communication/mqtt.ts
@packages/coordination/src/communication/message.ts
@packages/coordination/src/communication/topics.ts
@packages/coordination/src/discovery/registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement heartbeat publisher and tracker with 4-miss offline detection</name>
  <files>packages/coordination/src/lifecycle/heartbeat.ts, packages/coordination/src/lifecycle/index.ts, packages/coordination/src/communication/topics.ts, packages/coordination/src/index.ts</files>
  <action>
    Update packages/coordination/src/communication/topics.ts:
    - Add export function agentHeartbeat(agentId: string): string
    - Return topic format: agent/{agentId}/heartbeat
    - Add to subscription patterns if needed

    Create packages/coordination/src/lifecycle/heartbeat.ts:
    - Import EventEmitter from 'events'
    - Import crypto from 'node:crypto'
    - Import { MqttClientMinimal } from '../discovery/registry.js'
    - Import { Topics } from '../communication/topics.js'
    - Import type { Database } from 'better-sqlite3'
    - Import type { MessageEnvelope } from '../communication/message.js'

    Export interface HeartbeatConfig {
      agentId: string
      interval: number // 30 seconds per STAT-01
      mqttClient: MqttClientMinimal
      db?: Database.Database
    }

    Export interface AgentHeartbeat {
      agentId: string
      lastSeen: number
      missedCount: number
      status: 'online' | 'offline'
    }

    Export class HeartbeatPublisher:
      constructor(config: HeartbeatConfig)
      start(): void - setInterval for config.interval, publish immediately
      stop(): void - clearInterval
      setStatus(status: 'idle' | 'busy' | 'error'): void - update currentStatus and publish immediately
      private publish(): void - create MessageEnvelope with type='heartbeat', payload={agentId, status}, qos=0 (COMM-07), publish to Topics.agentHeartbeat(config.agentId)

    Export class HeartbeatTracker extends EventEmitter:
      HEARTBEAT_INTERVAL_MS = 30000
      MISSED_THRESHOLD = 4 (CONTEXT.md locked decision - DISC-04)
      OFFLINE_TIMEOUT_MS = 120000 (2 minutes)

      constructor(db?: Database.Database)
      recordHeartbeat(agentId: string): void - update Map, set lastSeen=now, missedCount=0, emit 'agent-online' if was offline, update database if db provided
      private checkMissedHeartbeats(): void - setInterval every 30s, calculate missed intervals, mark offline if >= MISSED_THRESHOLD, emit 'agent-offline' event
      getAgentStatus(agentId: string): AgentHeartbeat | undefined
      getAllStatuses(): Map<string, AgentHeartbeat>
      loadFromDatabase(db: Database.Database): void - SELECT * FROM agent_status, populate Map on startup
      private updateDatabase(agentId: string, status: string, timestamp: number): void - UPSERT into agent_status table

    Export factory function createHeartbeatPublisher(config: HeartbeatConfig): HeartbeatPublisher
    Export factory function createHeartbeatTracker(db?: Database.Database): HeartbeatTracker

    Per RESEARCH.md Pattern 2, CONTEXT.md "Heartbeat Monitoring" decision, DISC-04 (4 missed beats), STAT-01 (30s interval).
  </action>
  <verify>
    cd packages/coordination && npm run build
    grep -r "agentHeartbeat" src/communication/topics.ts
    grep -r "class HeartbeatPublisher" src/lifecycle/heartbeat.ts
    grep -r "class HeartbeatTracker" src/lifecycle/heartbeat.ts
    grep -r "MISSED_THRESHOLD.*4" src/lifecycle/heartbeat.ts
    grep -r "HEARTBEAT_INTERVAL_MS.*30000" src/lifecycle/heartbeat.ts
  </verify>
  <done>
    Heartbeat publisher sends 30-second messages via MQTT with QoS 0. Tracker marks agents offline after 4 missed beats (2 minutes). Both classes support database persistence for crash recovery.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create systemd service template and supervisor helper</name>
  <files>packages/coordination/src/lifecycle/supervisor.ts, packages/coordination/src/lifecycle/index.ts, config/supervisor/openclaw-agent@.service</files>
  <action>
    Create packages/coordination/src/lifecycle/supervisor.ts:
    - Import { readFileSync } from 'node:fs'
    - Import { join } from 'node:path'

    Export interface SupervisorConfig {
      agentId: string
      nodePath: string // Default: '/usr/bin/node'
      scriptPath: string // Default: '/opt/openclaw-swarm/dist/agent.js'
      user: string // Default: 'openclaw'
      group: string // Default: 'openclaw'
      workingDirectory: string // Default: '/opt/openclaw-swarm'
    }

    Export const SYSTEMD_TEMPLATE: string = `
    [Unit]
    Description=OpenClaw Swarm Agent (%i)
    After=network.target mosquitto.service
    Requires=mosquitto.service

    [Service]
    Type=simple
    User={{USER}}
    Group={{GROUP}}
    WorkingDirectory={{WORKING_DIR}}
    ExecStart={{NODE_PATH}} {{SCRIPT_PATH}} --agent-id=%i
    Restart=on-failure
    RestartSec=1s
    RestartSteps=5
    RestartMaxDelaySec=30s
    StartLimitIntervalSec=60s
    StartLimitBurst=5
    StandardOutput=journal
    StandardError=journal
    SyslogIdentifier=openclaw-%i

    # Graceful shutdown: allow time to finish current task
    TimeoutStopSec=30s
    KillMode=mixed
    KillSignal=SIGTERM

    [Install]
    WantedBy=multi-user.target
    `

    Export function generateSystemdService(config: Partial<SupervisorConfig> = {}): string {
      const defaults = { nodePath: '/usr/bin/node', scriptPath: '/opt/openclaw-swarm/dist/agent.js', user: 'openclaw', group: 'openclaw', workingDirectory: '/opt/openclaw-swarm' }
      const final = { ...defaults, ...config }
      return SYSTEMD_TEMPLATE
        .replace('{{USER}}', final.user)
        .replace('{{GROUP}}', final.group)
        .replace('{{WORKING_DIR}}', final.workingDirectory)
        .replace('{{NODE_PATH}}', final.nodePath)
        .replace('{{SCRIPT_PATH}}', final.scriptPath)
    }

    Export function installSystemdService(agentId: string, config?: Partial<SupervisorConfig>): string {
      // Returns the generated service file content
      // Note: Actual installation requires sudo and systemctl commands (human step)
      const serviceContent = generateSystemdService(config)
      return serviceContent
    }

    Export const INSTALL_INSTRUCTIONS = `
    # Install OpenClaw Agent systemd service
    sudo cp config/supervisor/openclaw-agent@.service /etc/systemd/system/
    sudo systemctl daemon-reload
    sudo systemctl enable openclaw-agent@minerva
    sudo systemctl start openclaw-agent@minerva

    # Check status
    sudo systemctl status openclaw-agent@minerva

    # View logs
    journalctl -u openclaw-agent@minerva -f
    `

    Create config/supervisor/openclaw-agent@.service:
    - Copy systemd template from SYSTEMD_TEMPLATE above
    - Use actual values: User=openclaw, Group=openclaw, WorkingDirectory=/opt/openclaw-swarm
    - ExecStart=/usr/bin/node /opt/openclaw-swarm/dist/agent.js --agent-id=%i
    - Include Restart=on-failure, RestartSteps=5, RestartMaxDelaySec=30s
    - Include TimeoutStopSec=30s for graceful shutdown per LIFE-03

    Per RESEARCH.md Pattern 3, CONTEXT.md "Agent Supervision" decision (exponential backoff), LIFE-01 (auto-start), LIFE-02 (auto-restart).
  </action>
  <verify>
    cd packages/coordination && npm run build
    grep -r "Restart=on-failure" config/supervisor/openclaw-agent@.service
    grep -r "RestartSteps.*5" config/supervisor/openclaw-agent@.service
    grep -r "TimeoutStopSec.*30" config/supervisor/openclaw-agent@.service
    grep -r "generateSystemdService" src/lifecycle/supervisor.ts
  </verify>
  <done>
    systemd service template created with Restart=on-failure, exponential backoff (1s->2s->4s->8s->16s->30s max), and 30-second graceful shutdown timeout. Template supports instantiation for any agent ID.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement graceful shutdown handler with task completion</name>
  <files>packages/coordination/src/lifecycle/shutdown.ts, packages/coordination/src/lifecycle/index.ts, packages/coordination/src/index.ts</files>
  <action>
    Create packages/coordination/src/lifecycle/shutdown.ts:
    - Import type { MqttClientMinimal } from '../discovery/registry.js'
    - Import type { AgentDiscovery } from '../discovery/registry.js'
    - Import type { HeartbeatPublisher } from './heartbeat.js'
    - Import { logger } from '../errors/logger.js'

    Export interface ShutdownConfig {
      mqttClient?: MqttClientMinimal
      agentDiscovery?: AgentDiscovery
      heartbeatPublisher?: HeartbeatPublisher
      gracefulShutdownTimeout: number // 30 seconds per systemd service file
    }

    Export class GracefulShutdown {
      private isShuttingDown = false
      private pendingTasks = new Set<string>()

      constructor(private config: ShutdownConfig) {
        this.setupSignalHandlers()
      }

      registerTask(taskId: string): void {
        this.pendingTasks.add(taskId)
      }

      completeTask(taskId: string): void {
        this.pendingTasks.delete(taskId)
      }

      private setupSignalHandlers(): void {
        process.on('SIGTERM', () => this.shutdown('SIGTERM'))
        process.on('SIGINT', () => this.shutdown('SIGINT'))
      }

      private async shutdown(signal: string): Promise<void> {
        if (this.isShuttingDown) {
          return
        }

        this.isShuttingDown = true
        logger.info({ event: 'shutdown_initiated', signal }, 'Received shutdown signal')

        try {
          // Wait for pending tasks to complete (up to timeout)
          const startTime = Date.now()
          while (this.pendingTasks.size > 0) {
            const elapsed = Date.now() - startTime
            if (elapsed >= this.config.gracefulShutdownTimeout) {
              logger.warn({ pendingTasks: this.pendingTasks.size }, 'Graceful shutdown timeout, forcing exit')
              break
            }
            logger.info({ pending_tasks: this.pendingTasks.size }, 'Waiting for tasks to complete')
            await new Promise(resolve => setTimeout(resolve, 1000))
          }

          // Stop heartbeat publisher
          if (this.config.heartbeatPublisher) {
            this.config.heartbeatPublisher.stop()
            logger.info('Heartbeat publisher stopped')
          }

          // Unregister agent (clear retained message)
          if (this.config.agentDiscovery) {
            await this.config.agentDiscovery.unregisterAgent(this.config.agentDiscovery.getAgentId?.() || 'unknown')
            logger.info('Agent unregistered from discovery')
          }

          // Disconnect MQTT
          if (this.config.mqttClient) {
            await this.config.mqttClient.end()
            logger.info('MQTT client disconnected')
          }

          logger.info('Graceful shutdown complete')
          process.exit(0)
        } catch (error) {
          logger.error({ error }, 'Error during graceful shutdown')
          process.exit(1)
        }
      }
    }

    Export function createGracefulShutdown(config: ShutdownConfig): GracefulShutdown {
      return new GracefulShutdown(config)
    }

    Create packages/coordination/src/lifecycle/index.ts:
    - Re-export all from heartbeat.ts: HeartbeatPublisher, HeartbeatTracker, createHeartbeatPublisher, createHeartbeatTracker, HeartbeatConfig, AgentHeartbeat
    - Re-export all from supervisor.ts: generateSystemdService, installSystemdService, INSTALL_INSTRUCTIONS, SupervisorConfig, SYSTEMD_TEMPLATE
    - Re-export all from shutdown.ts: GracefulShutdown, createGracefulShutdown, ShutdownConfig

    Update packages/coordination/src/index.ts:
    - Add: export * from './lifecycle/index.js';

    Per RESEARCH.md "Graceful Shutdown Handler" example, CONTEXT.md "Agent Supervision" decision (finish in-progress tasks), LIFE-03 (graceful shutdown).
  </action>
  <verify>
    cd packages/coordination && npm run build
    grep -r "class GracefulShutdown" src/lifecycle/shutdown.ts
    grep -r "SIGTERM.*SIGINT" src/lifecycle/shutdown.ts
    grep -r "pendingTasks" src/lifecycle/shutdown.ts
    grep -r "export.*from.*lifecycle" src/index.ts
  </verify>
  <done>
    Graceful shutdown handler waits for pending tasks up to 30-second timeout before exit. Handles SIGTERM from systemd and SIGINT (Ctrl+C). Stops heartbeat, unregisters agent, closes MQTT connections. All lifecycle modules exported from package.
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. **Build passes:** cd packages/coordination && npm run build (no TypeScript errors)
2. **Heartbeat topic exists:** agentHeartbeat function in topics.ts returns "agent/{id}/heartbeat"
3. **Publisher class:** HeartbeatPublisher with start, stop, setStatus methods
4. **Tracker class:** HeartbeatTracker with 4-miss threshold (MISSED_THRESHOLD = 4)
5. **Database persistence:** Tracker loads from and saves to agent_status table
6. **systemd template:** openclaw-agent@.service with Restart=on-failure, RestartSteps=5
7. **Graceful shutdown:** GracefulShutdown with SIGTERM/SIGINT handlers, waits for pending tasks
8. **Exports:** All lifecycle modules re-exported from index.ts

Critical requirements validation:
- DISC-04: 4 missed heartbeats = offline (MISSED_THRESHOLD constant)
- LIFE-01: Auto-start on boot (systemd service with WantedBy=multi-user.target)
- LIFE-02: Auto-restart on crash (Restart=on-failure with exponential backoff)
- LIFE-03: Graceful shutdown (GracefulShutdown class with 30s timeout)
- STAT-01: 30-second heartbeat interval (HEARTBEAT_INTERVAL_MS = 30000)
</verification>

<success_criteria>
1. Agents publish heartbeat messages every 30 seconds with status (idle/busy/error)
2. Agent missing 4 consecutive heartbeats is marked offline (2-minute timeout)
3. Agents start automatically on machine boot via systemd service
4. Agents that crash are automatically restarted with exponential backoff
5. Agents gracefully shutdown on SIGTERM, completing current task if possible
6. Heartbeat tracking state persists in database for crash recovery
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-state-lifecycle/02-02-SUMMARY.md`
</output>
