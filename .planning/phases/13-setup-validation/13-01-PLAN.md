---
phase: 13-setup-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - SETUP-01

must_haves:
  truths:
    - "Root package.json contains workspaces field"
    - "Workspaces includes packages/* pattern"
    - "examples/ directory excluded from workspaces"
    - "npm install creates symlinks in node_modules/@openclaw-swarm/"
    - "Coordination package accessible via @openclaw-swarm/coordination"
  artifacts:
    - path: "package.json"
      provides: "Root workspace configuration"
      contains: '"workspaces":'
      contains: 'packages/*'
  key_links:
    - from: "package.json"
      to: "packages/coordination"
      via: "npm workspaces symlink"
      pattern: "workspaces.*packages/\\*"
---

<objective>
Add npm workspaces configuration to root package.json

Purpose: Enable monorepo package linking so coordination package can be imported locally during development

Output: Root package.json with workspaces configuration
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-setup-validation/13-CONTEXT.md
@.planning/phases/13-setup-validation/13-RESEARCH.md
@package.json
@packages/coordination/package.json
</context>

<tasks>

<task type="auto">
  <name>Add workspaces configuration to root package.json</name>
  <files>package.json</files>
  <action>
Read the existing /home/gr3gg0rk/openclaw-swarm/package.json file. Add the following fields to the root JSON object:

1. Add "workspaces" field with value ["packages/*"] - this includes coordination package but excludes examples/ per 13-CONTEXT.md decision
2. Ensure "private" field is set to true (prevents accidental publish of root package)
3. Add "scripts" section if not present, with these scripts:
   - "setup": "node scripts/setup.mjs"
   - "build": "npm run build --workspaces"
   - "dev": "npm run dev --workspaces"
   - "test": "npm run test --workspaces"

The root package.json currently has minimal content (dependencies and devDependencies only). Preserve any existing fields, just add the missing ones.

Do NOT modify packages/coordination/package.json - it is already properly configured.
  </action>
  <verify>
<automated>cat package.json | grep -A1 '"workspaces"' | grep 'packages/\*'</automated>
<manual>Verify package.json contains workspaces, private, and scripts fields with correct values</manual>
  </verify>
  <done>Root package.json contains workspaces configuration with packages/* pattern, private: true, and npm scripts for setup/build/dev/test</done>
</task>

</tasks>

<verification>
After completing this plan, verify that workspaces configuration is active by running:
```bash
npm install
```

Verify symlinks were created:
```bash
ls -la node_modules/@openclaw-swarm/
```

Expected output should show a symlink to packages/coordination
</verification>

<success_criteria>
- Root package.json contains "workspaces": ["packages/*"]
- Root package.json has "private": true
- Root package.json has setup, build, dev, test scripts
- Running npm install creates node_modules/@openclaw-swarm/coordination symlink
</success_criteria>

<output>
After completion, create `.planning/phases/13-setup-validation/13-01-SUMMARY.md`
</output>
