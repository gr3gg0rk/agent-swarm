---
phase: 05-integration-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/src/delegation/worker.ts
  - packages/coordination/src/delegation/retry.ts
autonomous: true
requirements:
  - HARD-04
  - ERRO-05
  - ERRO-04

must_haves:
  truths:
    - "WorkerTaskExecutor checks task status at start and during execution"
    - "WorkerTaskExecutor aborts execution when task status becomes 'paused'"
    - "WorkerTaskExecutor requests guidance via GuidanceRequest for ambiguous errors"
    - "RetryManager publishes MQTT notification when task exhausts all retries"
    - "No TODO stubs remain in worker.ts or retry.ts"
  artifacts:
    - path: "packages/coordination/src/delegation/worker.ts"
      provides: "Pause status handling and GuidanceRequest integration"
      exports: ["WorkerTaskExecutor"]
    - path: "packages/coordination/src/delegation/retry.ts"
      provides: "Minerva notification on retry exhaustion"
      exports: ["RetryManager"]
  key_links:
    - from: "packages/coordination/src/delegation/worker.ts"
      to: "packages/coordination/src/memory/throttle.ts"
      via: "TaskStatus 'paused' checks in executeTask()"
      pattern: "task\\.status === 'paused'"
    - from: "packages/coordination/src/delegation/worker.ts"
      to: "packages/coordination/src/delegation/guidance.ts"
      via: "GuidanceRequest instantiation in constructor"
      pattern: "new GuidanceRequest\\("
    - from: "packages/coordination/src/delegation/retry.ts"
      to: "packages/coordination/src/communication/topics.ts"
      via: "MQTT topic for task_failed notification"
      pattern: "Topics\\.(taskFailed|guidanceRequest|agentError)"
---

<objective>
Complete all three cross-phase integrations identified in v1.0 audit:

1. Wire pause handling in WorkerTaskExecutor (HARD-04) - Check task status at start and during execution, abort if paused
2. Connect GuidanceRequest to worker error handling (ERRO-05) - Replace TODO with actual GuidanceRequest integration
3. Implement Minerva notification on retry exhaustion (ERRO-04) - Replace console.error with MQTT message

Purpose: Close integration gaps from previous phases, ensuring all components are wired together for production use.

Output: WorkerTaskExecutor handles memory throttling, workers request guidance for ambiguous errors, Minerva receives structured failure notifications.
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-integration-wiring/05-RESEARCH.md
@.planning/phases/04-error-handling-recovery/04-02-PLAN.md

# Component References
@packages/coordination/src/delegation/worker.ts
@packages/coordination/src/delegation/guidance.ts
@packages/coordination/src/delegation/retry.ts
@packages/coordination/src/memory/throttle.ts
@packages/coordination/src/communication/topics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire pause handling in WorkerTaskExecutor</name>
  <files>packages/coordination/src/delegation/worker.ts</files>
  <action>
Modify WorkerTaskExecutor in packages/coordination/src/delegation/worker.ts to handle 'paused' task status:

1. **Add pause check at task start** (in executeTask method, after line 203):
   - Get task from taskQueue using getTask(taskId)
   - Check if task.status === 'paused'
   - If paused: log info message and return early (skip execution)

2. **Add pause monitoring during execution** (in executeTask method, after starting timeout monitoring around line 283):
   - Create setInterval that runs every 1000ms (1 second)
   - In interval callback: get current task from taskQueue
   - If task.status === 'paused': clearInterval, throw Error('Task paused by memory throttle controller')
   - Store interval ID so it can be cleaned up

3. **Cleanup pause monitor** (in success and error paths around lines 302-305, 358-362, 385-389):
   - Add clearInterval(pauseCheckInterval) to all cleanup locations
   - Ensure interval is cleared when task completes, fails, or is cancelled

4. **Store interval reference** (as class field):
   - Add private pauseCheckIntervals: Map<string, NodeJS.Timeout> field to class
   - Use taskId as key to track multiple tasks
   - Clean up interval from map when cleared

Pattern reference: ThrottleController sets task status to 'paused' in throttle.ts lines 88-92.

DO NOT:
- Do not add new dependencies
- Do not modify ThrottleController (it already sets 'paused' status correctly)
- Do not check ThrottleController directly (read task status from TaskQueue)
  </action>
  <verify>
1. Read packages/coordination/src/delegation/worker.ts
2. Confirm executeTask method has pause status check at start (pattern: task.status === 'paused')
3. Confirm executeTask method has setInterval checking pause status every 1000ms
4. Confirm pauseCheckIntervals map exists and is used
5. Confirm clearInterval is called in all cleanup locations (success, failure, cancellation)
6. Run: grep -n "paused" packages/coordination/src/delegation/worker.ts (should show multiple matches)
  </verify>
  <done>
WorkerTaskExecutor checks task status at start of executeTask() and returns early if paused. During execution, task status is monitored every 1 second and task aborts with descriptive error if status becomes 'paused'. Pause monitoring interval is cleaned up in all code paths (success, failure, cancellation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Connect GuidanceRequest to worker error handling</name>
  <files>packages/coordination/src/delegation/worker.ts</files>
  <action>
Modify WorkerTaskExecutor to integrate GuidanceRequest for ambiguous error handling:

1. **Add GuidanceRequest import** (at top of file, around line 24):
   - Import GuidanceRequest type from './guidance.js'
   - Verify it's already imported as type (should be line 24)

2. **Add guidanceRequest field to constructor** (around line 136-143):
   - Add optional parameter: guidanceRequest?: GuidanceRequest
   - Store as protected field: this.guidanceRequest = guidanceRequest
   - This allows dependency injection for testing

3. **Implement requestGuidanceIfNeeded method** (replace lines 409-430):
   - Keep the ambiguous pattern matching (lines 410-423 are correct)
   - Replace TODO block (lines 425-429) with:
     - Check if this.guidanceRequest exists
     - If exists: call await this.guidanceRequest.requestGuidance(taskId, error.message)
     - Log the returned guidance or timeout
     - If guidance is empty string (timeout): log warning and proceed with default behavior
     - If guidance is not empty: log info and apply guidance (or store for later use)

4. **Call requestGuidanceIfNeeded in error paths**:
   - In handleFailure method (around line 329), after classifying error
   - Add call: await this.requestGuidanceIfNeeded(error, taskId)
   - Only call for permanent errors (ambiguous situations are typically permanent)

Pattern reference: GuidanceRequest class is fully implemented in guidance.ts with 30-second timeout and empty string on timeout (lines 139-195).

DO NOT:
- Do not create new GuidanceRequest instance (inject via constructor)
- Do not block task execution indefinitely on guidance timeout
- Do not modify guidance.ts (it's already complete)
  </action>
  <verify>
1. Read packages/coordination/src/delegation/worker.ts
2. Confirm constructor accepts guidanceRequest parameter and stores it
3. Confirm requestGuidanceIfNeeded method calls this.guidanceRequest.requestGuidance()
4. Confirm TODO comment at line 426 is removed
5. Confirm handleFailure calls requestGuidanceIfNeeded for appropriate errors
6. Run: grep -n "TODO.*GuidanceRequest" packages/coordination/src/delegation/worker.ts (should return empty)
  </verify>
  <done>
WorkerTaskExecutor constructor accepts optional GuidanceRequest parameter. requestGuidanceIfNeeded() method calls guidanceRequest.requestGuidance() for ambiguous errors, logs response, and handles timeout case (empty string). TODO comment removed. Method is called from handleFailure for permanent errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Minerva notification on retry exhaustion</name>
  <files>packages/coordination/src/delegation/retry.ts</files>
  <action>
Modify RetryManager to send MQTT notification when task exhausts all retries:

1. **Add MqttClient and Topics imports** (at top of file, after line 17):
   - Add: import type { MqttClient } from '../communication/mqtt.js';
   - Add: import { Topics } from '../communication/topics.js';
   - Add: import type { MessageEnvelope } from '../communication/message.js';
   - Verify uuid import already exists (line 17)

2. **Add mqttClient field to constructor** (around line 104-111):
   - Add private parameter: private mqttClient: MqttClient
   - Store in constructor (existing constructor should be modified)

3. **Replace notifyExhausted TODO** (replace lines 259-284):
   - Create MessageEnvelope with:
     - messageId: uuidv4()
     - idempotencyKey: uuidv4()
     - from: 'retry-manager'
     - to: 'minerva'
     - type: 'task_failed'
     - timestamp: Date.now()
     - payload: { taskId, agentId: task?.assignedAgent, error: { type: errorType, message: error.message, reason: `Task failed after ${task?.maxRetries ?? 3} retries` } }
     - qos: 1 (at-least-once delivery)
     - retain: false
   - Publish envelope to Topics.agentError('minerva') or Topics.guidanceRequest()
   - Remove console.error (replace with MQTT publish)
   - Keep task status update to 'failed'

4. **Update createRetryManager function** (around line 335-341):
   - Add mqttClient parameter
   - Pass to constructor

Pattern reference: WorkerTaskExecutor.sendResult() shows MQTT publish pattern (worker.ts lines 472-487). Topics.guidanceRequest() exists for Minerva communication (topics.ts line 57).

DO NOT:
- Do not use console.error for production failure notification (must use MQTT)
- Do not create new topic (use existing Topics.guidanceRequest() or Topics.agentError())
- Do not modify task status update logic (keep that intact)
  </action>
  <verify>
1. Read packages/coordination/src/delegation/retry.ts
2. Confirm MqttClient, Topics, MessageEnvelope imports added
3. Confirm constructor accepts mqttClient parameter
4. Confirm notifyExhausted method creates MessageEnvelope and publishes via MQTT
5. Confirm TODO comment at line 276 is removed
6. Confirm console.error is replaced with mqttClient.publish()
7. Run: grep -n "TODO.*Minerva\|TODO.*notification" packages/coordination/src/delegation/retry.ts (should return empty)
  </verify>
  <done>
RetryManager constructor accepts MqttClient parameter. notifyExhausted() method creates MessageEnvelope with task_failed type and publishes via MQTT to Minerva topic. TODO comment removed. console.error replaced with structured MQTT notification. Task status still updated to 'failed'.
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. **Pause Handling Integration (HARD-04)**:
   - grep -n "paused" packages/coordination/src/delegation/worker.ts
   - Confirm executeTask checks status at start
   - Confirm setInterval monitors status during execution
   - Confirm cleanup in all code paths

2. **GuidanceRequest Integration (ERRO-05)**:
   - grep -n "guidanceRequest" packages/coordination/src/delegation/worker.ts
   - Confirm constructor accepts parameter
   - Confirm requestGuidanceIfNeeded() calls GuidanceRequest.requestGuidance()
   - No TODO stubs related to guidance

3. **Minerva Notification (ERRO-04)**:
   - grep -n "mqttClient.publish" packages/coordination/src/delegation/retry.ts
   - Confirm notifyExhausted() publishes MQTT message
   - No TODO stubs related to Minerva notification

4. **No TODO Stub Check**:
   - grep -rn "TODO.*GuidanceRequest\|TODO.*Minerva\|TODO.*pause" packages/coordination/src/delegation/
   - Should return empty (all TODOs resolved)

5. **Import Validation**:
   - npm run build (if exists) or tsc --noEmit (if TypeScript configured)
   - Should have no type errors
</verification>

<success_criteria>
Phase 5 is complete when:

1. WorkerTaskExecutor handles 'paused' task status from ThrottleController
   - Checks status at start, returns early if paused
   - Monitors status every 1 second during execution
   - Aborts execution if task becomes paused

2. GuidanceRequest is connected to worker error handling
   - WorkerTaskExecutor.requestGuidanceIfNeeded() calls GuidanceRequest.requestGuidance()
   - No TODO stubs for guidance integration

3. Minerva notification is sent when task exhausts all retries
   - RetryManager.notifyExhausted() publishes MQTT message
   - Message envelope uses 'task_failed' type
   - No TODO stubs for Minerva notification

4. All integration gaps from v1.0 audit are closed
   - grep for TODO stubs returns empty
   - Code compiles without type errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-wiring/05-01-SUMMARY.md` documenting:
- Changes made to worker.ts (pause handling, GuidanceRequest integration)
- Changes made to retry.ts (Minerva notification)
- TODO stubs removed
- Verification results
</output>
