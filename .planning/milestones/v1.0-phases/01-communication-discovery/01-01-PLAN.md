---
phase: 01-communication-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/package.json
  - packages/coordination/src/communication/mqtt.ts
  - packages/coordination/src/communication/message.ts
  - packages/coordination/src/communication/topics.ts
  - packages/coordination/src/communication/index.ts
  - packages/coordination/tsconfig.json
autonomous: true
requirements:
  - COMM-02
  - COMM-05
  - COMM-06
  - COMM-07
  - HARD-01
  - HARD-02
  - HARD-05

must_haves:
  truths:
    - "Agent can connect to MQTT broker on griak-brain:1883"
    - "Agent can publish messages with QoS 1 (tasks) and QoS 0 (heartbeats)"
    - "Agent receives messages on subscribed topics"
    - "Message envelope includes messageId, idempotencyKey, from, to, type, timestamp, payload"
    - "Messages over 1KB use MessagePack serialization"
    - "MQTT.js client auto-reconnects on connection loss"
    - "Mosquitto broker configured with <10MB RAM limit"
  artifacts:
    - path: "packages/coordination/package.json"
      provides: "Node.js package configuration for coordination layer"
      contains: "mqtt@^5.0.0, uuid@^11.0.0, msgpackr@^0.6.0, eventemitter3@^6.0.0"
    - path: "packages/coordination/src/communication/mqtt.ts"
      provides: "MQTT client wrapper with auto-reconnect"
      exports: ["MqttClient", "connectToBroker"]
      min_lines: 80
    - path: "packages/coordination/src/communication/message.ts"
      provides: "Message envelope types and interfaces"
      contains: "interface MessageEnvelope, type MessageType"
      min_lines: 40
    - path: "packages/coordination/src/communication/topics.ts"
      provides: "Topic hierarchy constants"
      contains: "const Topics, const Subscriptions"
      min_lines: 30
    - path: "config/mosquitto.conf"
      provides: "Mosquitto broker configuration"
      contains: "memory_limit 10M"
  key_links:
    - from: "packages/coordination/src/communication/mqtt.ts"
      to: "mqtt://griak-brain:1883"
      via: "mqtt.connect() with clientId from config"
      pattern: "mqtt\\.connect\\("
    - from: "packages/coordination/src/communication/message.ts"
      to: "packages/coordination/src/communication/mqtt.ts"
      via: "MessagePack.encode/decode for envelope serialization"
      pattern: "MessagePack\\.(encode|decode)"
    - from: "packages/coordination/src/communication/topics.ts"
      to: "MQTT broker"
      via: "Topic strings used in publish/subscribe"
      pattern: "agent/.*/(command|result)"
---

<objective>
Create the MQTT message bus foundation that enables reliable inter-agent communication with proper QoS levels, message envelope structure, and serialization.

**Purpose:** Phase 1 requires agents to send messages to specific agents by ID (COMM-02) with appropriate QoS levels (COMM-06, COMM-07) while respecting hardware constraints (HARD-01, HARD-02). This plan establishes the communication layer that all agent-to-agent messaging depends on.

**Output:**
- `packages/coordination/` npm package with MQTT client wrapper
- Message envelope types with idempotency support
- Topic hierarchy constants for directed and broadcast messaging
- Mosquitto broker configuration optimized for Pi 2B
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-communication-discovery/01-CONTEXT.md
@.planning/phases/01-communication-discovery/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create coordination package structure and dependencies</name>
  <files>
    packages/coordination/package.json
    packages/coordination/tsconfig.json
  </files>
  <action>
    Create the coordination layer package structure:

    1. Create `packages/coordination/package.json` with:
       - name: "@openclaw-swarm/coordination"
       - version: "0.1.0"
       - type: "module"
       - main: "./dist/index.js"
       - types: "./dist/index.d.ts"
       - exports: { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } }
       - dependencies: mqtt@^5.0.0, uuid@^11.0.0, msgpackr@^0.6.0, eventemitter3@^6.0.0
       - devDependencies: typescript@^5.9.3, @types/node@^22.19.11
       - scripts: build: "tsc", dev: "tsc --watch"

    2. Create `packages/coordination/tsconfig.json` with:
       - compilerOptions.target: "ES2022"
       - compilerOptions.module: "Node16"
       - compilerOptions.moduleResolution: "Node16"
       - compilerOptions.outDir: "./dist"
       - compilerOptions.rootDir: "./src"
       - compilerOptions.declaration: true
       - compilerOptions.declarationMap: true
       - compilerOptions.sourceMap: true
       - compilerOptions.strict: true
       - include: ["src/**/*"]
       - exclude: ["node_modules", "dist"]

    These are per RESEARCH.md "Standard Stack" section. DO NOT use alternatives.
  </action>
  <verify>
    ls packages/coordination/package.json packages/coordination/tsconfig.json
  </verify>
  <done>
    package.json and tsconfig.json exist with all required dependencies configured
  </done>
</task>

<task type="auto">
  <name>Implement MQTT client wrapper with auto-reconnect</name>
  <files>
    packages/coordination/src/communication/mqtt.ts
  </files>
  <action>
    Create `packages/coordination/src/communication/mqtt.ts`:

    Export a `connectToBroker()` function that:
    - Accepts config: { brokerUrl: string, clientId: string, username?: string, password?: string }
    - Calls `mqtt.connect()` with options from RESEARCH.md code examples:
      * clientId from config (e.g., 'minerva', 'worker-1')
      * clean: true
      * connectTimeout: 4000
      * reconnectPeriod: 1000
      * qos: 1 (default QoS level)
    - Returns a Promise<MqttClient> that resolves on 'connect' event
    - Sets up event listeners: connect, error, reconnect, message
    - Emits custom events using EventEmitter3 for message handling

    Export a `MqttClient` wrapper class that:
    - Wraps mqtt.Client instance
    - Provides publish(topic, payload, options) method with:
      * MessagePack encoding for payloads >1KB (HARD-05)
      * Default qos: 1, retain: false
      * Support for qos: 0 override for heartbeats (COMM-07)
    - Provides subscribe(topic, options) method
    - Provides unsubscribe(topic) method
    - Provides end() method for graceful disconnect

    Per CONTEXT.md locked decision: Use human-readable client IDs from static config.
    Per RESEARCH.md: Auto-reconnect is handled by MQTT.js with reconnectPeriod: 1000.

    DO NOT implement custom TCP protocol or alternative transports.
  </action>
  <verify>
    grep -E "export.*connectToBroker|export.*class MqttClient" packages/coordination/src/communication/mqtt.ts
  </verify>
  <done>
    mqtt.ts exports connectToBroker function and MqttClient class with auto-reconnect
  </done>
</task>

<task type="auto">
  <name>Define message envelope types and topic hierarchy</name>
  <files>
    packages/coordination/src/communication/message.ts
    packages/coordination/src/communication/topics.ts
    packages/coordination/src/communication/index.ts
  </files>
  <action>
    Create `packages/coordination/src/communication/message.ts`:
    - Export MessageType enum: 'task' | 'result' | 'heartbeat' | 'error' | 'discovery' | 'status'
    - Export MessageEnvelope interface per RESEARCH.md Pattern 1:
      * messageId: string (UUID - unique message identifier)
      * idempotencyKey: string (UUID - deduplicates re-deliveries for COMM-04)
      * correlationId?: string (UUID - links response to request)
      * from: string (Agent ID)
      * to?: string (Target agent ID, undefined for broadcast)
      * type: MessageType
      * timestamp: number (Unix timestamp ms)
      * payload: unknown (MessagePack or JSON)
      * qos?: 0 | 1 (Override default)
      * retain?: boolean

    Create `packages/coordination/src/communication/topics.ts`:
    - Export Topics object with factory functions per RESEARCH.md Pattern 2:
      * agent(agentId: string) => `agent/${agentId}`
      * agentCommand(agentId: string) => `agent/${agentId}/command`
      * agentResult(agentId: string) => `agent/${agentId}/result`
      * agentError(agentId: string) => `agent/${agentId}/error`
      * swarmDiscovery: 'swarm/discovery'
      * agentDiscovery(agentId: string) => `swarm/agents/${agentId}`
      * swarmStatus: 'swarm/status'
      * swarmEvents: 'swarm/events'
      * response(agentId: string) => `agent/${agentId}/response`
    - Export Subscriptions object with patterns:
      * allWorkersResults: 'agent/+/result'
      * workerCommands(agentId: string) => `agent/${agentId}/command`
      * allAgents: 'swarm/discovery'
      * agentStates: 'swarm/agents/#'

    CRITICAL: Never start topics with `/` (RESEARCH.md Anti-Patterns).

    Create `packages/coordination/src/communication/index.ts`:
    - Re-export from mqtt.ts, message.ts, topics.ts
  </action>
  <verify>
    grep -E "MessageType|MessageEnvelope" packages/coordination/src/communication/message.ts
    grep -E "Topics|Subscriptions" packages/coordination/src/communication/topics.ts
  </verify>
  <done>
    Message types and topic hierarchy defined with proper exports
  </done>
</task>

<task type="auto">
  <name>Create Mosquitto broker configuration for Pi 2B</name>
  <files>
    config/mosquitto.conf
  </files>
  <action>
    Create `config/mosquitto.conf` per RESEARCH.md Pitfall 5:

    Configuration sections:
    1. Memory limits:
       - memory_limit 10M (HARD-02 requirement)
       - max_queued_messages 100
       - max_connections -1 (no limit for 4-agent swarm)

    2. Logging:
       - log_dest stderr
       - log_type error
       - log_type warning

    3. Socket optimization:
       - socket_domain ipv4 (IPv4 only)

    4. Persistence (optional - comment out for now):
       - # persistence false (can be enabled later)

    Include comments explaining each setting relative to HARD-02 (<10MB RAM).

    DO NOT add authentication yet (Phase 1 scope - single trusted network).
  </action>
  <verify>
    grep "memory_limit 10M" config/mosquitto.conf
  </verify>
  <done>
    Mosquitto configuration with 10MB memory limit created
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Build the package: `cd packages/coordination && npm install && npm run build`
2. Verify no TypeScript errors
3. Check that dist/ directory contains index.js, index.d.ts
4. Verify MQTT client options include reconnectPeriod: 1000 for auto-reconnect
5. Verify topic strings do NOT start with `/` (anti-pattern check)
</verification>

<success_criteria>
- Coordination package builds without errors
- MQTT client wrapper supports connect, publish, subscribe, unsubscribe, end
- Message envelope includes all required fields (messageId, idempotencyKey, etc.)
- Topic hierarchy provides agent-specific and broadcast topics
- Mosquitto.conf enforces 10MB memory limit
- All exports available from @openclaw-swarm/coordination package
</success_criteria>

<output>
After completion, create `.planning/phases/01-communication-discovery/01-01-SUMMARY.md` with:
- Actual files created with line counts
- npm packages installed with versions
- Any deviations from RESEARCH.md patterns (should be none)
- Verified Mosquitto configuration settings
</output>
