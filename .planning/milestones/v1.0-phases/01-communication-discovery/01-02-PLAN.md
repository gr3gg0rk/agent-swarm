---
phase: 01-communication-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coordination/src/discovery/registry.ts
  - packages/coordination/src/discovery/query.ts
  - packages/coordination/src/discovery/types.ts
  - packages/coordination/src/discovery/index.ts
  - config/agents.yaml
autonomous: true
requirements:
  - COMM-01
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-05

must_haves:
  truths:
    - "Agent can register itself on startup with ID, role, and capabilities"
    - "Agent registration is persisted in retained MQTT messages"
    - "New agent starting immediately receives all existing agent registrations"
    - "Minerva can query which agents are available and their capabilities"
    - "Duplicate agent IDs are rejected on registration attempt"
    - "Agent can unregister on graceful shutdown"
    - "Static configuration defines the 4 known machines"
  artifacts:
    - path: "packages/coordination/src/discovery/registry.ts"
      provides: "Agent registration with retained MQTT messages"
      exports: ["AgentDiscovery", "registerAgent", "unregisterAgent"]
      min_lines: 80
    - path: "packages/coordination/src/discovery/query.ts"
      provides: "Query available agents and their capabilities"
      exports: ["queryAvailableAgents", "getAgentById"]
      min_lines: 50
    - path: "packages/coordination/src/discovery/types.ts"
      provides: "Agent registration types"
      contains: "interface AgentRegistration, type AgentRole"
      min_lines: 30
    - path: "config/agents.yaml"
      provides: "Static configuration of known machines"
      contains: "griak-brain, griak-server, griak-worker-1, griak-worker-2"
  key_links:
    - from: "packages/coordination/src/discovery/registry.ts"
      to: "MQTT broker"
      via: "Topics.agentDiscovery(agentId) with retain: true"
      pattern: "publish.*retain.*true"
    - from: "packages/coordination/src/discovery/query.ts"
      to: "packages/coordination/src/discovery/registry.ts"
      via: "Query retained messages from swarm/agents/#"
      pattern: "swarm/agents/#"
    - from: "config/agents.yaml"
      to: "packages/coordination/src/discovery/registry.ts"
      via: "Config loaded to validate agent IDs"
      pattern: "agents\\.yaml"
---

<objective>
Implement agent discovery using MQTT retained messages so agents can find each other across machines without polling or centralized coordination.

**Purpose:** COMM-01 requires agents to discover each other using retained messages. DISC-01 requires self-registration with ID, role, capabilities. DISC-03 requires registration persistence for crash recovery. This plan delivers the discovery layer that enables Minerva to query available agents (DISC-02) and validates against static machine configuration (DISC-05).

**Output:**
- Agent registration system with retained MQTT messages
- Query interface for discovering available agents
- Duplicate agent ID rejection
- Static agent configuration file
</objective>

<execution_context>
@/home/gr3gg0rk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gr3gg0rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-communication-discovery/01-CONTEXT.md
@.planning/phases/01-communication-discovery/01-RESEARCH.md
@.planning/phases/01-communication-discovery/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Define agent registration types and static configuration</name>
  <files>
    packages/coordination/src/discovery/types.ts
    config/agents.yaml
  </files>
  <action>
    Create `packages/coordination/src/discovery/types.ts`:
    - Export AgentRole type: 'orchestrator' | 'worker' (CONTEXT.md locked decision)
    - Export AgentRegistration interface per RESEARCH.md Pattern 3:
      * agentId: string (human-readable: 'minerva', 'worker-1', 'worker-2')
      * role: AgentRole (separate from agent ID per CONTEXT.md)
      * capabilities: string[] (e.g., ['code', 'test', 'debug'])
      * hostname: string (griak-brain, griak-server, griak-worker-1, griak-worker-2)
      * ip?: string (optional, for direct connectivity)
      * version: string (coordination layer version)
      * startedAt: number (Unix timestamp ms)

    Create `config/agents.yaml`:
    - Define the 4 known machines per DISC-05 and CONTEXT.md:
      * agents array with entries for:
        - agentId: minerva, hostname: griak-brain, role: orchestrator
        - agentId: worker-1, hostname: griak-server, role: worker
        - agentId: worker-2, hostname: griak-worker-1, role: worker
        - agentId: worker-3, hostname: griak-worker-2, role: worker
    - Include optional capabilities array (empty or commented for now)
    - Include comment: "Static configuration per DISC-05 - validates agent IDs on registration"

    DO NOT add dynamic capability declaration (v2 feature ROUT-01).
  </action>
  <verify>
    grep -E "AgentRole|AgentRegistration" packages/coordination/src/discovery/types.ts
    grep -E "griak-brain|griak-server|griak-worker" config/agents.yaml
  </verify>
  <done>
    Agent types defined with role separated from ID, static config created
  </done>
</task>

<task type="auto">
  <name>Implement agent registration with retained messages</name>
  <files>
    packages/coordination/src/discovery/registry.ts
  </files>
  <action>
    Create `packages/coordination/src/discovery/registry.ts`:

    Export AgentDiscovery class that:
    - Constructor accepts MqttClient instance (from plan 01-01)
    - Has registerAgent(registration: AgentRegistration) method that:
      * Validates agentId against config/agents.yaml (DISC-05)
      * Rejects duplicate IDs by checking if topic already has retained message
      * Creates MessageEnvelope with:
        - messageId: uuidv4()
        - idempotencyKey: uuidv4()
        - from: registration.agentId
        - type: 'discovery'
        - timestamp: Date.now()
        - payload: registration
      * Publishes to Topics.agentDiscovery(agentId) with:
        - qos: 1 (reliable discovery)
        - retain: true (DISC-03 - persists for crash recovery)
      * Throws error if duplicate detected (CONTEXT.md locked decision)
    - Has unregisterAgent(agentId: string) method that:
      * Publishes empty payload (Buffer.alloc(0)) to clear retained message
      * Uses qos: 1, retain: true
      * Called on graceful shutdown

    Per RESEARCH.md Pattern 3: Use retained messages for instant discovery without polling.
    Per CONTEXT.md: Duplicate agent IDs must be rejected - new agent fails to start.

    DO NOT use periodic broadcast for registration (retained message pattern per RESEARCH.md).
  </action>
  <verify>
    grep -E "registerAgent|unregisterAgent" packages/coordination/src/discovery/registry.ts
  </verify>
  <done>
    Agent registration with duplicate rejection and retained message persistence
  </done>
</task>

<task type="auto">
  <name>Implement agent discovery query interface</name>
  <files>
    packages/coordination/src/discovery/query.ts
    packages/coordination/src/discovery/index.ts
  </files>
  <action>
    Create `packages/coordination/src/discovery/query.ts`:

    Export functions that query retained messages from swarm/agents/#:

    1. queryAvailableAgents(mqttClient: MqttClient): Promise<AgentRegistration[]>
       - Subscribes to 'swarm/agents/#' with qos: 1
       - Queries all retained messages (mosquitto API or subscription cache)
       - Maps messages to AgentRegistration[]
       - Filters by type === 'discovery'
       - Returns array of available agents

    2. getAgentById(mqttClient: MqttClient, agentId: string): Promise<AgentRegistration | null>
       - Queries specific topic: swarm/agents/${agentId}
       - Returns registration or null if not found

    3. getAgentsByRole(mqttClient: MqttClient, role: AgentRole): Promise<AgentRegistration[]>
       - Queries all agents, filters by role
       - Enables role-based routing for DISC-02

    Create `packages/coordination/src/discovery/index.ts`:
    - Re-export from types.ts, registry.ts, query.ts
    - Re-export from communication plan (Topics, MessageEnvelope) for convenience

    Per DISC-02: Minerva can query which agents are available and their capabilities.
    Per COMM-01: Discovery works across machines via MQTT retained messages.
  </action>
  <verify>
    grep -E "queryAvailableAgents|getAgentById|getAgentsByRole" packages/coordination/src/discovery/query.ts
  </verify>
  <done>
    Query interface for discovering agents by ID, role, and all available
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Update packages/coordination/src/index.ts to export discovery module
2. Build the package: `cd packages/coordination && npm run build`
3. Verify no TypeScript errors
4. Test scenario: Can register minerva, then query and find it
5. Test scenario: Attempting duplicate registration throws error
6. Verify retained message pattern in registry.ts (retain: true)
</verification>

<success_criteria>
- Agent registration publishes to retained MQTT topic
- Duplicate agent IDs are rejected with error
- Query functions return AgentRegistration[] from retained messages
- Role-based filtering works for orchestrator vs worker
- Static config validation prevents unknown agent IDs
- All exports available from @openclaw-swarm/coordination
</success_criteria>

<output>
After completion, create `.planning/phases/01-communication-discovery/01-02-SUMMARY.md` with:
- Actual files created with line counts
- Retained message pattern implementation details
- Duplicate rejection logic description
- Query interface capabilities verified
</output>
